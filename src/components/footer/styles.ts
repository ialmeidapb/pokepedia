import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
  footer: {
    position: "fixed",
    left: "0",
    bottom: "0",
    width: "100%",
    backgroundColor: "red",
    color: "white",
    textAlign: "center",
    display: "flex",
    justifyContent: "center",
  },
}));
